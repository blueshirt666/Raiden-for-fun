# TapeOut #260 — 4 位与 8 位拼接进位加

链上任务 ID `#260`（题库序号 293）。`S[8:0] = B[7:0] + A[3:0]`，A 零扩展。

## 为什么是这道题

`K_task ≡ 参考实现门数`，`C_ref ≡ 门数 × 深度³`（在 15 道题上逐条核对通过）。
所以 q 顶满时：

    性价比 = H / 烧掉的NAND = (n + 4K)/n = 1 + 4 × (参考门数 / 我的门数)

即"性价比最高"等价于"参考实现面积虚胖倍数最大"。#260 用 938 门做一件 52 门
的事，虚胖 18×，是第二名（#253，7.2×）的 2.5 倍。

## 交付

| 文件 | 门数 | 深度 | C = A·d³ | C_ref/C | 烧 NAND | H(P=1) | H/n |
|---|---|---|---|---|---|---|---|
| `tapeout_260_v1_*` | 52 | 18 | 303,264 | 48× | 52 | 3,804 | 73.2 |
| `tapeout_260_v2_*` | 64 | 14 | 175,616 | 83× | 64 | 3,816 | 59.6 |
| `tapeout_260_v3_*` | 83 | **8** | **42,496** | **345×** | 83 | 3,835 | 46.2 |

三版 q 都顶满 4.0，H 几乎相同 —— 压深度**不增加收益**，只买夺席位的安全边际。
V1 = 最省 NAND。V3 = C 最小，夺席位最稳。

## 对比榜主

巨兽榜首 #1083 链上权重 22,992 = (b* + 938×4)×6 ⇒ **b* = 80 门，q 已顶满**。
q 顶满只给出 C_主 ≤ C_ref/4 = 3,664,062（d ≤ 35），几乎无约束，**深度未知**。
夺取要求 C 严格更小，于是各版的夺取条件是：

| 变体 | 榜主深度 ≥ 此值才夺得下 |
|---|---|
| V1 | 16 |
| V2 | 13 |
| V3 | **9** |

V3 只在"榜主已经做到 80 门 / 深度 ≤ 8"时失手 —— 那需要对方已经做过同一套
深度优化。若榜主是教科书 8 位行波加法器（68 门/深 18）+ 12 个输出缓冲 = 80 门，
则 C_主 ≈ 466,560，三版全部夺得下。

**流片前仍须自行核对榜主实际 C。**

## V3 的两个代数恒等式

深度从 18 压到 8，靠的是这两条：

1. **吸收律** `g ⊆ p`（p 取 OR-propagate）：
   `G(i:j) = p_i ∧ (g_i + G(i-1:j))`，故
   `¬G(i:j) = NAND(p_i, NAND(¬g_i, ¬G(i-1:j)))`
   —— 每级只 2 层 NAND，且**补码进、补码出**，不用来回取反。

2. **分配律**：`c4 = G32 + P32·G10 = (G32+P32) ∧ (G32+G10)`
   末级变成 AND，于是 `¬c4 = NAND(X,Y)` 一个门直接出，比 `NOT(c4)` 早整整一层。
   又因 `g2 ⊆ p2`，`G32+P32 = g3 + p3·p2`，再省一层。

结果：`¬c4 @ 深度 5`、`c4 @ 深度 6`，高 4 位用条件和（两个候选值预先算好，
c4 到了只做 2 层 MUX）落在深度 8。低 4 位同理走补码 MUX。

`a_first` / `b_first` 是两种引脚排布假设（4 位操作数在前 / 8 位在前），
功能相同，选与画布实际引脚顺序匹配的那个。

## 结构（V1，52 门）

- bit0：半加器 5 门
- bit1..3：全加器 9 门 ×3 —— `t=NAND(p,c)` 同时供进位和和，复用 `¬g`
- bit4..7：高半只是把 c4 当增量传播，退化成半加器 5 门 ×4
- s8 = c8，直接复用高半最后一个进位门，零额外开销

9 个输出天然就是末尾 9 个门（LSB→MSB），**不需要任何缓冲门**。

## 验证

    python3 check.py roundtrip.py

- 4096 个输入向量穷举，失败 0
- 零死门
- 输出末尾断言通过
- `roundtrip.py` 从 .blif 文件反向解析重新仿真，并与 .hex 交叉核对
